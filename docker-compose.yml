version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: meet-clone-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: meet-clone
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    networks:
      - meet-clone-network

volumes:
  mongodb_data:
    driver: local
  mongodb_config:
    driver: local

networks:
  meet-clone-network:
    driver: bridge
